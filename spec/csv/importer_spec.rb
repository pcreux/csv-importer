require 'spec_helper'

describe Csv::Importer do
  it 'has a version number' do
    expect(Csv::Importer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
